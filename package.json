{
  "name": "graphql-mission-control",
  "version": "1.0.0",
  "description": "🔭 GraphQL Mission Control — live demo for GraphQL End to End: Your First Query in 20 minutes.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "graphql": "^16.9.0",
    "graphql-yoga": "^5.10.0"
  },
  "engines": {
    "node": ">=18"
  }
}
